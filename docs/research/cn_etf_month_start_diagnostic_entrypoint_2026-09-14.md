# 月初固定窗口的一次诊断入口

[提案](cn_etf_month_start_liquidity_proposal_2026-09-14.md)已经完成独立来源用途自审、合成验收及冻结登记。本次评审由当前任务完成，没有独立人员复核，也没有将历史来源假设认证为事实。单独的一次调度决定只允许此条件性佣金诊断；普通研究预算仍为0。

固定登记ID为`a9f17ed49b79a0dbb1610296c00908299e496c8c1dbd4ecfc86e41d0bcdda456`，登记字节SHA256为`a92f7a97bcf8274f51c8717b5b9a3df9d2ae9ef19e4f282bdd2fe3735d9b42f0`。它绑定11份输入、18份实现、运行环境、2020年1月至2024年5月53个月及一次尝试目录，不继承政策不确定性或居民偏好额度。

入口先核对登记、来源指纹和新调度决定，再生成匹配当前分支的启动检查。预检不构造真实条件、不解码ETF行情。真正执行时先独占写入不可复用的尝试记录，然后才解码限定年份、标的和日期的价格并计算；失败、中断或残留部分记录均不能自动重跑。执行使用已核验的内存快照，避免检查之后原文件变动替换输入。

准备入口为`scripts/prepare_cn_etf_month_start_diagnostic.py`，执行入口为`scripts/run_cn_etf_month_start_diagnostic.py`；后者默认只预检，显式`--execute`才消费一次权限。真实输入执行要求已提交的干净工作树；合成来源只在隔离测试中使用。已经创建的登记不能覆盖。

新增纯计算、日历适配、登记、来源、执行及启动检查共45项测试，相关114项及最终配置回归32项全部通过，去重后为132项。覆盖旧额度拒绝、多个专门权限冲突、状态扩大、输入变化、读取顺序、失败和中断不可重跑、快照一致性，以及费用和日期边界。项目审计扫描3221个文件通过；可维护性基线通过，既有技术债仍在。

下一步以最终提交完成一次预检及一次执行，完整保留全部周期、毛选择差额和0/5/10元最低佣金情景，按冻结规则收束。结果保存在`data/reports/cn_etf_month_start_commission_diagnostic_20260914/`；本地来源及验收在`data/reports/etf_monetization_20260911/calendar_liquidity_review_20260914/`。本次不认证净账户、风险达标、净正EV或真实向前天数，不读取2026ETF留出。
