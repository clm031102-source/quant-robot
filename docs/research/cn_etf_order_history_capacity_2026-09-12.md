# 离线订单历史容量与Windows监控文件并发修复

历史持仓接口的单次访问被拒后，本轮转向已有执行工程缺口。完成了订单和回报规模的有界诊断、历史状态复制优化，以及Windows并发文件替换问题的复现和修复。没有生成研究因子、连接券商或增加真实向前模拟天数。

## 实测负载与范围

本次使用离线基础账本、10万元合成初始现金、0.5基点比例佣金及每单5元最低佣金。买卖交替，每笔100份、假设价格4元，分40份与60份两次明确成交；另有接受回报、迟到接受回报和重复成交去重。所有生命周期步骤分别走SQLite事务并保留原来的完整同步落盘设置。这个资金数仅用于容量负载，不改变用户的资金或准入风控配置，也不代表完整受控运行链的容量已通过。

| 相同90秒负载预算 | 修改前 | 修改后 |
| --- | --- | --- |
| 完整处理订单 | 537笔 | 665笔 |
| 唯一明确回报 | 2,148条 | 2,660条 |
| 第401—500笔单笔中位耗时 | 197.6毫秒 | 125.4毫秒 |
| 最后区间单笔中位耗时 | 200.2毫秒（501—537） | 133.1毫秒（601—665） |
| 最终现金/持仓与逐笔费用一致 | 通过 | 通过 |
| 重开状态一致，重送旧成交不重复记账 | 通过 | 通过 |

这两个同机样本分开执行，后台负载未隔离。修改后有一次约1.9秒的延迟尖峰；没有达到预设最多1,000笔，也不能从单次90秒样本推断长期吞吐保证。修改前第500笔的剖析中，通用深复制约占76%的剖析耗时，支持定位重复复制的来源；带剖析开销的数值不替代正常运行延迟。

首版负载脚本误把FILLED当成状态回报接口支持的输入，被正确拒绝，失败样本保留。修正后的迟到ACCEPTED回报只能保留已由成交决定的FILLED状态，不能倒退订单状态。

## 复制优化及不变的账本要求

连接缓存中的终态订单和去重回报转为递归不可修改的内部记录，后续命令共享未变化的记录。迟到成交、未知状态、撤单、超时或完整对账需要改变订单时，先复制受影响的那笔。公开快照和普通读取仍返回可独立编辑的完整副本，不把内部共享对象交给调用者修改。

事件仍逐条追加。命令构造时的临时修改不发布为真实状态，提交失败或回滚不发布缓存；重启、外部连接提交和未跟踪写入仍按原规则重新验证。没有删除终态订单、截断去重历史或取消完整对账。当前状态总量仍随历史增长，极大规模下的内存、全量快照和重开成本尚需单独验收。

## Windows并发文件替换

旧提交29027af9的远程检查仅Windows/Python 3.13一组失败，失败断言是工作进程提前退出，子进程错误当时被丢弃，无法从该日志独立确定唯一根因。本机额外复现：普通读者保持监控文件打开时，原子替换返回WinError 5，旧文件保持完整。仅增加共享删除读取标志的尝试在本机仍失败，已保留失败样本并撤回该方案。

最终修复位于原子文件发布：仅对Windows错误5、32、33，在复用已经同步到磁盘的同一临时文件前提下最多尝试6次、等待5次，每次10毫秒；持续失败照常抛出。其他错误（包括磁盘空间不足）不重试、不吞掉。并发读者关闭后，真实Windows替换已能完成；持续占用反例仍保留旧文件并清理临时文件。监控身份、失联期限和暂停条件均未放宽。Windows共享与删除/改名语义参考[Microsoft说明](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilew)。

失联进程测试另保留子进程错误信息，并先确认至少两轮正常完成后再终止监督者，使失败更可诊断。远程新提交是否通过须单独追踪，不能用本机复现直接替代远程结论。

## 证据与接续

新增13个测试方法，覆盖共享记录隔离、迟到回报、旧成交去重、完整对账、回滚和原子文件替换。最终364项相关检查全部通过，十步运行演练通过，合成初始3,000元扣除10元买卖费用后为2,990元，没有把分红和折算重复计作收益。构建、项目审计和可维护性基线检查通过，证据以`data/reports/etf_monetization_20260911/order_history_capacity_stage/`及`order_history_accounting_drill/`为准。首次宽回归启动遇到数值库线程分配内存失败，限制该次测试进程的数值库线程后完成，启动失败日志保留。两次负载样本分别位于`order_receipt_capacity_before_v2/`和`order_receipt_capacity_after_v1/`；原始合成账本和日志不提交Git。

前一提交5778043b的远程运行34673977016已完成成功；它早于本批修改，不能替代当前提交的远程验收。29027af9的旧失败记录仍保留。

接续优先补上大订单历史下的受控运行、外部写入及持续故障验收；历史来源继续保持未通过，等待有权限或已许可资料的新证据。上述工程成果不构成盈利证据，人工整体成熟度仍约30%。关联：[当前落地方案](etf_monetization_execution_plan_2026-09-11.md)、[历史持仓访问结果](cn_etf_flow_source_contract_review_2026-09-12.md)。
