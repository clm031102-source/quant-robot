# 界面费用与页面检查的远程失败诊断

提交`bb0cb4ab`的[远程检查34735816499](https://github.com/clm031102-source/quant-robot/actions/runs/34735816499)中，Windows 3.11运行3265个测试后报告一项错误：界面费用浏览器测试的Node子进程超过30秒。其余五组检查成功。日志没有保留超时前子进程输出，尚不能判断是启动、测试体还是退出阶段卡住，也不能归因于费用映射运行逻辑。

本地相同测试原样执行通过，耗时0.200秒；本次只补充诊断：超时仍然失败，30秒限制不变，错误显示已捕获的标准输出、错误输出及命令，浏览器测试启动时报告Node版本和平台。未修改业务代码、交易风控或费用计算，也没有重跑远程任务来抹去旧失败。

新增一项诊断验证，覆盖超时输出为字节和文本两种情况。修改前两种情况均没有所需失败消息；修改后连同界面费用、执行条件和研究准入共24项Python测试通过。恢复后的完整日志为`data/reports/etf_monetization_20260911/option_mechanism_20260913/timeout_diagnostic_resumed.log`。此前中断日志保留为未完成证据，未计作通过。

随后读取既有提交`2e3f958d`和`d390530e`的远程状态，两者各六组检查全部成功；这两者不含本次诊断修改，也不能证明旧超时根因消失。若后续再失败，使用新日志定位具体阶段；没有新失败证据前，不改时限、不反复做相同短演练。本问题与此前监控进程测试的旧失败分别记录。

## 第二次远程超时后的启动调整

2026-09-13约13:25 UTC取得`d8453b8d`的[远程检查34758387299](https://github.com/clm031102-source/quant-robot/actions/runs/34758387299)及失败日志：Windows 3.13的3278项测试中，同一个Node费用测试超过30秒，标准输出和错误输出均未捕获任何内容，另外五组成功。它证明诊断已生效且没有提供启动阶段细节，不能据此认定测试体开始执行，更不能断言超时唯一根因。

连续两次同类远程失败后，改为在Python已有的独立子进程中直接运行这个单一JavaScript测试文件，去掉额外的`--test`工作进程层。五个费用案例、断言、30秒限制和错误传播都保持不变，不跳过失败或自动重试。[Node执行模型说明](https://nodejs.org/api/test.html#test-runner-execution-model)解释了默认子进程隔离；本文件已经独立启动，无需为一个文件再建立一层进程。

本地直接启动实际执行五个案例并通过，连同费用、执行输入及研究准入共24个Python方法通过。另在隔离副本中故意把一个费用预期5改成6，实际Node进程以1退出并报告该断言失败，确认替代启动方式仍会拒绝错误。没有新增测试方法或修改生产代码；此处是减少启动层次并完成限定验证，尚未证明远程超时消失。日志、故障副本和证据保存在`data/reports/etf_monetization_20260911/etf_connect_mechanism_20260913/`。

## 不同的页面检查失败与证据表述修正

约13:52 UTC读取`27e475a1`的[检查34759564638](https://github.com/clm031102-source/quant-robot/actions/runs/34759564638)：Windows 3.13在3292项测试中出现一项页面检查失败，当时Windows 3.12仍在运行、另外四组成功。失败只显示报告状态不等于passed，未输出失败项，不能推断它也是Node启动故障或归因于完整归档功能。

现在断言失败时列出具体失败项及证据，检查条件、5秒请求限制和失败结果保持不变。源码核对还发现HTTP检查报告会固定宣称桌面和手机已由浏览器视觉验证；实际脚本只检查HTTP内容及接口契约，因此改为明确“本检查未验证”。已完成的独立浏览器视觉证据仍单独保留，不由这个脚本自动认证。

最终版本的一个现有页面检查测试通过，未新增测试方法；旧远程故障根因仍未知。失败日志及限定验证位于`data/reports/etf_monetization_20260911/policy_uncertainty_mechanism_20260913/`。源码表述修正与远程失败诊断分别记账，不能称作旧故障已修复。
