# 政策不确定性月频诊断：一次准入

本次准入只允许执行已经固定的[历史毛收益诊断](cn_etf_policy_uncertainty_historical_diagnostic_2026-09-14.md)。它仍属于`policy_uncertainty_monthly_median_v1`经济假设，没有新增独立假设或净盈利认定。主研究预算保持0，七个关闭ETF家族和一个辅助项不重启，原一次预筛额度不复用。

## 已冻结的登记

- 登记身份：`381106288c5dfddf9d27d0b798d29f4e52ea843cb67a7e8e2cb28dc24c2f51b8`。
- 登记文件SHA256：`a584a1fabb56b1afb90d214fb8c5b1d1cad18ffed47ef8080f58d9a5c4fefb80`。
- 本地登记：`data/reports/cn_etf_policy_uncertainty_historical_diagnostic_20260914/registration.json`。
- 绑定14份输入及14份选定实现文件的字节指纹，并记录Python、平台及pandas、pyarrow、numpy版本；不能把这一清单称为整个操作系统或全部第三方代码的认证。
- 范围固定为510300、原54个月初交易日及C前七个自然日的ALFRED版本，构成53个完整收市至收市区间。终点不生成新信号，不扩至2026留出或追加月份。
- 毛诊断`mean((z_t-mean(z))*R_t)`非正或信号常量则收束；为正也只能评审后续账户研究。没有P值、置信区间、净账户、整手或盘中风险合规结论。

来源使用仍为带明确剩余假设的回顾分析：已知分红毛额在除息收市按分数份额再投资；未列示折算的不存在没有被证明。后期报告仅用于来源复核。全局`source_audit_verified=false`、真实账户费用未知、原真实报价路线挂起等结论均保留。

这是依据用户持续研究授权完成的用途及实现自审，不冒充独立人员复核。登记准备命令仅核验指纹和元数据，没有解码行情、计算真实信号或发放权限；经过本次审查后，由调度配置中的专门`monthly_diagnostic_decision`批准这一份登记的一次执行。

## 执行与中断合同

[准备命令](../../scripts/prepare_cn_etf_policy_uncertainty_diagnostic.py)不会覆盖已有登记。[运行命令](../../scripts/run_cn_etf_policy_uncertainty_diagnostic.py)默认仅检查；必须明确执行，并同时满足相同登记、调度决定、新鲜Quant PM门禁、当前分支及干净工作区。普通因子批次仍被拒绝，专门门禁只开放这一份月频诊断。

执行前读取并核验输入字节快照；先独占创建固定研究的尝试记录并刷盘，再解码行情和计算信号。计算使用已核验的快照，不重新打开原始行情或行动文件。政策信号保留CSV原始小数，避免浮点舍入改变中位数的等号判断。

同一个研究的尝试记录路径固定，修改登记内容或更换输出目录不自动补发额度。两个进程竞争时只允许一个取得额度；部分写入、异常及突然退出均不恢复为未使用。结果和终态文件不覆盖；只有结果文件存在且指纹一致，才写已完成状态。失败保留原因类型，进程突然退出时保留未完成状态，不自动重跑。

当前门禁检查可消费状态时也查看实际尝试记录，不能只相信调度JSON里尚未更新的计数。执行结束后另行记录结果和收束决定；不把本文件中的准入状态改写为已经完成。

## 验收证据

90项相关测试通过，27项新增，涵盖原门禁和来源采集回归、53期合成流程、真实命令检查与执行、两个真实进程竞争、强制退出、文件快照、原始小数精度及越界拒绝。测试中没有计算真实候选收益。

扩大验证曾出现一次并发路径误拒绝。随后在Windows路径实现中复现：父目录创建使错误从“路径不存在”变为“文件不存在”，同一路径可能保留`\\?\`前缀，字面比较因而误判越界。现只在比较时统一等价的盘符/UNC表示，实际IO仍使用解析后的路径；外部链接和带前缀的外部目标继续拒绝。原偶发失败没有保留解析前后的字符串，因此不能断言它的具体时序已被直接观测。该失败和确定性复现均保留。

证据保存在本地`data/reports/etf_monetization_20260911/monthly_diagnostic_entrypoint_20260914/`。截至本次准入，真实诊断未执行、真实向前模拟新增0日，正EV未证实。
