# 项目进度评估与接续工作清单

评估日期：2026-09-21。机器：`office_desktop`。本轮任务：`factor_review`。
用户已授权选择分支、修改、提交和推送。独立工作分支为
`codex/factor-review-project-progress-20260921`；主目录另有因子任务，后续修改使用独立 worktree。

## 当前结论

项目已完成大量研究控制、数据审计和离线执行工程，**尚无符合完整验收条件的策略**。
专题成果现已合入主线；当前关键缺口是合格候选、来源时点和费用证据。
增加批次数或重复短模拟不能替代这些缺口。
不使用文件数、提交数、测试数推算完成百分比，也不把旧报告的人工成熟度估计当作客观进度。

开始评估时同时检查两个实际版本，以下是合并前的基线：

| 版本 | 本次核对结果 | 解释 |
| --- | --- | --- |
| `main` / `origin/main`：`30ea14d` | 拉取后已同步；最后提交日期为2026-07-29 | 已集成研究框架与NAV失效控制 |
| `origin/codex/factor-review-etf-monetization-20260911`：`0b8984c` | 比main多141个提交；423个文件，39,546行增加、539行删除；最后提交为2026-09-14 | 重要成果尚待主线集成，不能遗忘或重新实现 |

远端专题分支的执行计划为 `docs/research/etf_monetization_execution_plan_2026-09-11.md`，
本地接续记录为 `data/reports/etf_monetization_20260911/progress.json`。
后续执行已将这141个提交并入本任务的隔离集成分支，处理README与索引冲突，
并保留原研究关闭记录。随后最终代码`351be99`通过六组远端检查，
[PR #3](https://github.com/clm031102-source/quant-robot/pull/3)已于北京时间17:30合入main，
主线提交`c02b453`的文件树与已测代码完全相同。未重跑任何已消费的研究授权。
随后GUI演示隔离修复通过六组矩阵，[PR #4](https://github.com/clm031102-source/quant-robot/pull/4)
于18:16合入main `e3204fc`；合并树与最终已测提交`7b100b1`一致。
共享按时点验收修复和期权关闭记录随后通过六组矩阵，PR #5于18:49合入main
`40627b6`；文件树与已测`4bf6dfb`一致，Windows 3.13完整日志确认3,776项测试全部通过。
披露资金流关闭及劳动来源范围随后通过六组矩阵，PR #6于19:06合入main
`0a99993`；合并树与最终`534565b`一致，Windows 3.12完整日志确认3,790项测试通过。
劳动风险关闭与首份真实v2验收总结随后通过六组矩阵，PR #7于19:35合入main
`a4a558b`；合并树与最终`091a4f2`一致，Windows 3.11完整日志确认3,805项测试通过。

## 分项进度

| 环节 | 已有成果 | 当前缺口与状态 |
| --- | --- | --- |
| 研究控制 | 预注册、单次授权、时点、成本、容量、重复因子和留出集控制已存在 | 主预算为0；仅允许新方向评审，没有可晋级候选 |
| 历史ETF来源 | 主线保留1,121,050条历史行情、NAV来源审计和1,069只PCF目标ETF证据 | 来源可取得不等于可研究；专题审计发现复权值是原价副本、部分身份及权益连续性仍需认证 |
| 来源采集 | 前向原件留存、版本及明确截止读取已实现；9月21日首份真实资金流v2完成固定两截止验收 | 19:00时两格未知、19:05收件后两格可读；来源仍未取得研究资格，不重解释旧v1或补造历史 |
| 账户与离线执行 | 最低佣金、每日估值、分红折算、更正、订单恢复、对账、跨日风控和失联监控已集成 | 限定合成验收已有记录；没有冻结候选的长期真实向前联验 |
| 后续研究 | 专题分支保留政策不确定性、居民偏好、月初和财政事件研究记录 | 分别存在拒绝、描述性结果、费用依赖或账户检验未通过；不能当作盈利策略或复用单次许可 |
| 纸面观察 | 工程工具和旧观察流程存在 | 新候选真实向前观察日为0；旧记录不能拼接成新业绩；20日、30笔及两类市场状态仍待满足 |
| 执行接口 | 本轮离线接口检查为 `schema_ready_execution_disabled` | 无券商连接、实盘账户读取、订单或实盘能力授权 |
| 工程质量 | 主线项目审计通过；维护性基线未退化；专题最新资金流回归本轮复核通过 | 主线13个超大模块；612个单元测试文件、2个集成测试文件、0个e2e文件；这些统计不代表专题分支全貌 |

主线配置中有7个明确关闭的ETF家族、3个零预算探索家族、1个辅助股票家族。
调度器显示8个止损触发，包含辅助股票家族，不能表述为8个明确关闭的ETF家族。
专题分支另有独立研究的关闭记录，应按对应授权和关闭包核对，不能仅数家族名称。

## 本轮已经推进

- 第四项固定美国方差风险检验已关闭：45个季度选中37个，18个为正，平均毛收益+1.4039%；全期相同持仓暴露的择时效果为-0.3960bp/交易日，固定95%区间为[-3.1387,+2.0586]bp/交易日，未满足预设下界。37个季度来自6段连续选中状态，未进入净账户或纸面。55份输入与14份实际运行代码指纹匹配，登记/占用/结果/消耗链一致；三份Git blob与运行文件仅有LF/CRLF差异，未放松指纹检查。固定实现`79b4b14`独立静态审查未发现重要缺陷，完整关闭版本`9ace23d`进入下一批整合，本任务不重跑该研究。

- 第三项固定劳动风险检验也已关闭：48个区间选中22个，平均毛收益+1.5833%，但2023年起仅2个选中区间，未满足预设后段最低数；22个区间仅来自两段连续宏观状态，不可当作22次独立交易。登记、占用、结果、消耗及复核五份指纹已匹配。未运行净账户、未增加纸面天数；完整关闭版本`c810a615`已整合，54项合成测试、3,316文件审计、编译、维护性和PM评审门禁通过，独立审查未发现重要缺陷。冻结生产代码和配置保持原指纹。

- 19:05完成首份未来资金流v2验收：一次实际请求，HTTP200、180,873字节，收件时间为19:05:20.988989，采集CLI结束后首个实际时钟19:05:21.182997固定为晚截止。19:00早截止跳过未来收件、两格均未知；晚截止两格可读、无来源读取阻断。冻结scope、占用、completion、current、原始响应及六份实现指纹全部匹配。最多两次reader已消费，不再重跑或替换批次；来源仍为`observed_unqualified`，研究准入为false、新纸面日为0。

- 后续在`codex/factor-review-flow-integration-20260921`隔离整合并发任务固定`e4d1b894`：披露资金流38个月仅3个正信号，平均毛收益+2.7302%，但样本数量和效应置信下界均未达预设条件，已关闭，不是净正EV证明。登记、占用、结果和消耗四份指纹均匹配，独立代码审查未发现重要缺陷；本次不重跑真实研究。另核对劳动力来源范围的60个唯一发布条目、57次新请求及3份复用与固定指纹一致，仅为来源采集范围审查，不授予收益检验。

- 已接入并发任务完整期权关闭记录`22a4363`（三个提交），保留单次执行消耗及失败状态。固定52区间中16个选中月，正收益占43.75%，平均含分红毛收益为-0.1009%；未进入净账户或纸面阶段。登记、占用、结果和outcome四份指纹一致，独立审查未发现重要问题；本集成不重跑市场实验。

- 同步main并核对未合并分支、既有接续记录和研究权限；保留专题分支成果。
- 使用项目Python 3.12.13。系统Python 3.14不符合项目声明的3.11–3.13支持范围。
- 运行Quant PM门禁：`ready`，模式为 `family_rotation_review_only`；普通因子批次仍被调度器阻止。
- 对官方历史PCF做4次跨市场/跨时期请求，对 `etf_basic` 做1次请求，五次均为 `permission_denied`。没有启动完整下载，也没有循环重试。
- 核对已有PCF目标包：1,069只ETF，含101只退市基金；跨市场交付契约及来源验收入口已具备。
- 在独立工作区检出专题分支精确版本 `0b8984c`，复核资金流v2、按时点读取及CLI的5组测试：**60项测试、33项子测试通过**。这是合成回归，不是新增来源日。
- 修正README、研究索引、调度说明和门禁说明中过时的状态及授权描述；门禁提示不再将所有新方向误称为动态同行因子。
- 集成审查发现并修复受限HTTP客户端的重复JSON键缺陷：此前同名字段可能掩盖分页或提供商拒绝。三个反例均先复现，再验证拒绝；采集链路保留失败占用和响应指纹，不生成成功投影、不继续请求、不重试。
- 修复中债、资金流采集CLI跨工作区重复占用问题。关联worktree通过Git公共目录定位主仓库，共用既有每日档案和失败占用；研究门禁仍在当前代码工作区执行。若工作区另有独立来源档案或Git身份损坏则拒绝，不忽略旧占用、不自动搬迁。真实本机两份入口只预览，均返回`archive_repo_root=F:/lhjqr`；未到采集时段、未取件。
- 将并发因子任务的`2e45dd0`账户成果接入本集成分支：当前默认本金10000元，旧显式实验参数保持可复现。当前离线接口保留单仓1000元、日亏60元和纸面回撤8%；放大这些接口风险限额会被拒绝。通用模拟引擎和旧GUI风险档位仍是可配置工具，不等于已取得当前账户的研究或执行准入。
- 合并后接续GUI诊断，复现默认演示建议会读取工作站真实候选报告的环境依赖。默认演示改用内置基线，默认操作记录和每日执行包不再作为演示决策输入；HTTP演示忽略报告目录覆盖，回执标记示例来源。内部显式合成fixture入口保持。进一步修复后续模拟请求误用真实数据源的问题：构造参数锁定标识前传入示例来源，两条模拟交接路径都保留来源。相关GUI、准入、隔离及建议链路152项测试通过。该修复消除了已证实的隐式资料依赖，不能据此宣称原60秒超时的完整根因已确定。

权限实测配置：`configs/cn_etf_external_data_unlock_review_20260921.json`。
原始结果留在本机 `F:/lhjqr/data/reports/cn_etf_external_data_unlock_review_20260921/`，
JSON SHA-256为 `e4d906586acd1ccda34f84efcf7e9f46ad6a69eefc465295998fbe9153bddd90`。
此次权限拒绝作为新的接续检查点；没有权限或来源变化时不再重复请求。

## 按优先级执行的工作列表

| 优先级 | 工作 | 依赖和完成条件 | 状态 |
| --- | --- | --- | --- |
| P0 | 统一状态入口，记录主线与专题差异 | 新入口覆盖两条分支；不再把旧paper-ready解释为当前策略可用 | 本轮完成 |
| P0 | 审查并集成专题分支 | 按账户/执行、来源/版本、研究门禁分组审查141个提交；核对最终提交CI；保留研究关闭记录 | 已完成：PR #3合入main `c02b453`，最终六组矩阵全部通过，完整日志确认3,758项测试通过 |
| P1 | 验收第一份未来资金流v2原件 | 严格执行已冻结的 `next_v2_source_acceptance_scope.json`，不重跑9月14日v1 | 9月21日19:05完成；早截止两格未知、晚截止两格可读，单次验收已消费；来源资格仍受限 |
| P1 | 积累中债来源原件 | 工作日18:05后、当天未尝试时最多一次；保留原件、收件时间、观察日及修订记录；不计算ETF收益或冒充历史发布版本 | 9月21日18:05:16原件已留存；HTTP200、13,544字节，仍为observed_unqualified；当天不再请求 |
| P1 | 取得可用历史PCF/历史映射 | 已启用接口权限，或本机有合法授权的沪深历史导出和字段字典；先通过结构、目标范围和时点验收 | 五次实测拒绝；等待实际来源变化，不自动购数 |
| P1 | 整理可执行研究输入口径 | 复用专题价格/权益审计；每个新提案先说明所需来源、时点、ETF暴露和否定条件，按阶段补足资格 | 可独立开展限定审查；不重建既有执行底座 |
| P2 | 固定一个独立假设 | 来源就绪后冻结公式、方向、期限、费用、资本、统计检验和停止规则，并绑定全部输入与实现指纹 | 前置门槛未满足，不生成新因子 |
| P2 | 完成真实账户口径验证 | 固定研究获准后验证只买入账户、整手、最低佣金、资金限制、样本外、成本压力与延迟 | 1万元默认账户及当前离线契约已接入；每边万五、最低0/5/10元仍为情景，实际费用及合格候选待落实 |
| P3 | 开始真实时间向前纸面观察 | 新候选通过全部门槛后，逐日记录至少20日、30笔、两种市场状态及不超过8%回撤；低频策略不为凑笔数交易 | 尚未开始；历史重放不累计天数 |
| P3 | 补关键集成覆盖、逐步拆大模块 | 围绕实际数据→账户→风控链路补验收；出现具体回归风险时再拆分，保持行为可核对 | 次于来源/候选关键路径，不以整理代码代替研究进展 |

## 后续运行规则

主目录存在其他任务。后续代码修改必须在独立工作区进行；运行任何数据或研究入口前，
核对该工作区的分支、版本、启动门禁及实际本地数据路径。大数据、原始响应、报告和凭据留在本机。

本分支的`scripts/capture_chinabond_observation.py`和
`scripts/capture_tushare_moneyflow_observation.py`已自动将关联worktree的来源档案定位到
主仓库`F:/lhjqr/data/reports/`。从独立工作区运行入口，使用当前实际分支名；先预览核对
`archive_repo_root`，再按原工作日时段和单次范围执行。此规则不替其他采集入口或独立克隆
建立共享占用，不授权额外日期或请求；已有分散档案须人工核对后再决定处置。

资金流按时点验收入口`scripts/review_tushare_moneyflow_asof.py`也使用同一主仓库档案根，
读取共享收件并将验收结果限定在该根的`data/reports`内；scope仍在当前代码工作区解析，
门禁仍检查当前工作区实际分支。这样不会把共享原件误判为路径越界，也不会把验收分散到工作区。
该入口已于9月21日19:05完成首份真实v2的两次固定读取，结果保存在共享
`data/reports/etf_monetization_20260911/moneyflow_contract_v2_20260914/first_future_v2_acceptance/`。
绑定代码为`40627b6`，completion SHA-256为
`1515217491ed5a5cf35f019c8415bdeb9b351cd4404cb3ec959261e324a96b1f`，原始响应SHA-256为
`3bc35a57c45784914a014c778d07c6ae65ebacccc1bfe1c1e9163286b995cd2e`。
该首次验收范围已经消费，不能再运行编排或手工reader；未来日常原件积累仍按既有每日合同。

持续推进优先复用专题分支现有实现与接续范围。闭合一项实际缺口才记为进展；
没有新来源、权限、费用或方法证据时，不重试旧拒绝请求、不重新筛选关闭家族、不读取2026留出收益。
已恢复原有ETF每小时心跳（`automation`），保留原任务和节奏，不另建重复任务。
保存状态已核对为 `ACTIVE`；新增共享证据占用检查、独立工作区要求和本次工作清单入口。
仅有实质进展、完成、失败或需用户处理时通知；定时运行需要桌面应用与电脑可用。

最低外部输入是来源访问或合法历史交付。券商费用等信息到实际账户验证阶段再按具体缺口取得，
不会因这些缺失停止可独立完成的审查工作。本任务始终为 research-to-paper；不连接券商、不读取实盘账户、不下单。

## 验证记录

- 主线工程审计：通过，扫描2,924个文件。
- 主线维护性审计：`baseline_passed_with_known_debt`，无基线回退；13个超大模块，最大模块13,982行。
- 离线执行接口：`schema_ready_execution_disabled`，所有外部执行边界为false。
- 专题精确版本 `0b8984c` 最新资金流回归：60项测试、33项子测试通过。
- 本轮最终门禁/调度/来源评审回归：24项测试、13项子测试通过；新增配置、文档入口与差异检查通过。
- 主线额外全量基线：2,475项，910.194秒，1项错误；GUI演示工作流的每日建议接口等待60秒超时。在空资料隔离工作区，同一测试未经修改以4.04秒通过。尚未证实原工作站超时根因，不将隔离通过记为修复，也不提高超时掩盖问题。
- 集成后门禁：7/7协议文件读取通过，`family_rotation_review_only`；研究预算仍为0。维护性基线通过，无退化；当前738个测试文件、504个源码模块，既有13个超大模块仍为已知债务。
- 重复JSON键修复：HTTP、采集、CLI、日历及分红44项测试、87项子测试通过；无真实网络调用。
- 跨工作区来源占用修复：临时真实Git worktree首先复现重复采集；修复后来源/CLI/版本52项测试、42项子测试通过；覆盖成功、失败占用、错误Git身份及独立档案冲突，未读取真实凭据。
- 安装包构建通过；集成项目审计扫描3,276个文件通过。
- 集成版全量回归：**3,746项通过，966.263秒，退出码0**，证据位于`integration/unittest.log`。该进程启动早于上述两项修复，最终修复另由44项及52项定向回归覆盖；不混称整套测试覆盖最终所有字节。
- 共享档案复核补充：检查所有已登记worktree；即便旧占用只在另一个工作区，从主目录或空的新工作区也会拒绝，不能忽略旧记录再次请求。两个来源均由同一回归覆盖；52项及42项子测试已对最终修复重新通过。
- 账户集成检查：固定`2e45dd0`独立审查未发现重要合并缺陷；账户、接口、GUI HTTP、逐日风险、执行费用和跨工作区来源32项测试、38项子测试通过。原GUI测试在本集成版本耗时4.14秒；原主目录超时仍没有根因证据，未宣称修复。
- 远端集成验收：最终`351be99`的Windows/Linux × Python 3.11–3.13六组矩阵全部成功；Windows 3.12与3.13日志均确认3,758项测试通过，构建、编译、项目审计及维护性全部通过。合并后的main `c02b453`与该代码树相同。[最终CI](https://github.com/clm031102-source/quant-robot/actions/runs/35580075488)，本机证据`integration/ci_head_351be99.json`。两个旧版本重复运行主动取消，不能当成待修复失败。
- GUI演示隔离：三个来源别名首先触发禁止读取的反例；后续请求来源错误也先复现再修复。GUI、准入、隔离及建议链路152项测试通过（24.876秒），覆盖HTTP报告路径覆盖、真实执行一次演示→模拟HTTP交接、来源与锁定标识、显式合成日报/台账及原演示工作流。维护性基线通过，两个已有大模块均缩小，未放宽行数上限。证据`integration/gui_demo_isolation_tests.log`；未使用真实市场资料复现原环境超时，仍保留该限制。

本机详细证据目录：`F:/lhjqr/data/reports/project_progress_20260921/`。
该目录和其他生成数据不进入Git。

- GUI最终远端验收：`7b100b1`六组矩阵全部成功，Windows 3.12完整日志确认3,761项测试通过；[CI运行](https://github.com/clm031102-source/quant-robot/actions/runs/35585061162)。PR #4合并树与已测树一致。
- 资金流验收共享路径修复：46项合成回归通过（3.096秒），覆盖共享原件早晚截止、共享输出、错误Git身份和输出越界；独立审查未发现实质问题。未执行真实来源验收。
- 期权关闭记录集成：期权诊断、单次许可、家族调度及PM门禁30项合成回归通过（1.205秒）；维护性基线通过。合并仅保留完整关闭状态，未执行真实诊断或重读市场结果。
- 后续披露资金流集成：上下界、资金流诊断与单次许可、既有期权、家族调度及PM门禁44项合成回归通过（2.912秒），新工作区门禁ready且普通主研究预算仍为0，维护性基线通过。固定`e4d1b894`的生产代码与配置直接保留，仅解决当前索引和接续文档冲突。
