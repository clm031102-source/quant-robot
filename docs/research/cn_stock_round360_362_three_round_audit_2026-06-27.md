# CN Stock Round360-362 Three-Round Audit

Date: 2026-06-27

Branch: `codex/factor-validation-cn-stock-24h-profit-sprint-20260627`

Scope: CN stock factor validation, research-to-review only. No broker, account, order, or live-trading access.

## Rounds Reviewed

| Round | Work | Status |
|---:|---|---|
| 360 | Add simulation shortlist replay checker | Completed |
| 361 | Replay current shortlist and fix source-column mismatch | Completed |
| 362 | Repeatability audit | Completed |

## Main Finding

The shortlist is now more repeatable than before this three-round block.

Before Round360, the candidate metrics were documented, but there was no single lightweight check that forced the config evidence to match the event-return files.

After Round361:

- every candidate has an explicit event-return source in the config;
- every event source can be replayed;
- all five candidates reproduce their config metrics within 0.005 tolerance;
- `safer_defensive_zz500` was corrected to use the final `overlay_return` stream.

## Important Correction

The replay checker caught a real issue:

`safer_defensive_zz500` was initially audited with `period_return`, but the correct final return stream is `overlay_return`.

Corrected safer metrics:

| Metric | Corrected Value |
|---|---:|
| Total return | +114.76% |
| Annualized return | +4.73% |
| Sharpe | 0.996 |
| Overlap Sharpe | 0.534 |
| Max drawdown | -14.94% |
| Leave-one-year minimum annualized return | +2.52% |
| Top three months log share | 48.26% |

This correction strengthens the process and weakens the `safer_defensive_zz500` lane's priority. It is safer on drawdown, but its return floor is weaker than the core defensive lane.

## Repeatability Status

Current status:

- Config-to-event replay: passed.
- Source-column mapping: explicit.
- Final holdout: sealed.
- Data/reports outputs: local only, not committed.
- Simulation-readiness: improved, but not complete.

Remaining gap:

The project still needs a repeatable raw-data-to-event generation entrypoint for the retained lanes. The current replay starts from event files, not from raw bars and factor inputs. That is acceptable for short-term audit continuity, but the simulation stage should not depend on hand-picked report artifacts forever.

## Direction Decision

Keep the same lane decisions:

| Lane | Decision |
|---|---|
| `primary_high_return` | Return-seeking lane |
| `primary_balanced_zz500_75` | Middle lane |
| `primary_defensive_zz500` | Default robustness lane |
| `primary_ps_filtered_defensive_zz500` | Defensive diagnostic lane |
| `safer_defensive_zz500` | Reference only |

Before any simulated-paper handoff, require:

1. `scripts/run_simulation_shortlist_replay.py` passes.
2. `scripts/run_shortlist_return_block_audit.py` is rerun with correct source columns.
3. The source event files are either regenerated by a formal entrypoint or explicitly treated as frozen research artifacts.
4. 2026 holdout remains sealed until the project enters final validation.

## Next Work

Round363 should choose one of two paths:

1. Build a raw-data-to-event generator for the four retained lanes.
2. If that is too costly for this sprint, continue mining only genuinely orthogonal sources that do not violate the startup blocked-family list.

Do not add more low-turnover variants until path 1 is handled.
